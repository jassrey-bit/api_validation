# Runner de Pruebas y Paralelización (suite pytest CLI legacy)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-xdist>=3.5.0
pytest-html>=4.1.1

# Cliente HTTP Asíncrono
httpx>=0.27.0

# Modelado de Datos y Configuración
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Comparación estructural de respuestas (regresión)
deepdiff>=7.0.0

# Servicio web (job pending/done/error consumido por qa-platform-web)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Generación de reportes HTML/PDF de la vía pytest CLI legacy
Jinja2>=3.1.3
playwright>=1.45.0
